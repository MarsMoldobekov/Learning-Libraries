<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/row_item_height"
    android:foreground="?attr/selectableItemBackground"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <ImageView
        android:id="@+id/image_view_avatar"
        android:layout_width="@dimen/row_item_imageView_size"
        android:layout_height="@dimen/row_item_imageView_size"
        android:layout_margin="@dimen/row_item_imageView_margin" />

    <TextView
        android:id="@+id/text_view_login"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/row_item_textView_marginStart" />

</LinearLayout>